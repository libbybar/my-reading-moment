PORT=7000

MONGODB_URI=

# "mock" (default) or "gemini"
LLM_PROVIDER=mock
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Set to "true" to enable temporary debug/timing logs (server/logs/timing.jsonl).
TIMING_LOG_ENABLED=false
