import styled from 'styled-components'

export const ChildHomeMessage = styled.p`
  font-family: ${(props) => props.theme.fonts.main};
  color: ${(props) => props.theme.colors.text};
  font-size: 18px;
  text-align: center;
  margin: 0;
`
